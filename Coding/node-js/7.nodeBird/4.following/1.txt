# 게시물, 이미지 업로드
multer설치
npm i multer

##
routes에 post.js생성
router.post("/img", isLoggedIn, upload.single("img"), afterUploadImage)
upload.single에 들어가는 img는 view의 main.html의 form에 들어가는 formData.append의 값과 같아야 한다.

### upload는 설정에 따라 여러개를 만들어도 된다.
routes/post의 img용 upload, 게시물용 upload2

##
controllers/post.js를 생성
함수들을 작성한다.